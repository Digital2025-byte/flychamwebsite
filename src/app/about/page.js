import AboutClient from './AboutClient';
export const metadata = {
    title: "About Us - FlyCham Airlines",
    description: "Discover the story behind FlyCham Airlines—our mission, values, and commitment to delivering a comfortable, safe, and reliable flying experience to destinations across the region.",
};

const About = () => {
    return (
        <AboutClient />
    );
};
export default About;
